# SAML and server base URL are generated at runtime by sonarqube-config-init into:
#   .vols/sonarqube/conf/sonar.properties
# That file is bind-mounted into the SonarQube container (see docker-compose.yml).
