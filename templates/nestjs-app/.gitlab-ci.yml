# ==========================================================================
# NestJS App Template — CI/CD Pipeline (v2 Auto DevOps)
# ==========================================================================
# Delegates build + deploy to the shared Auto DevOps pipeline config.
# Override individual jobs below as needed for this app.
# ==========================================================================

include:
  - project: "configs/auto-devops-pipeline"
    file: "/.gitlab-ci.yml"

# App-specific overrides go below.
# Example: extend the test job with real tests
# test:
#   script:
#     - npm ci
#     - npm test
