# Auto-provision Loki as the default Grafana datasource.
apiVersion: 1
datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://devtools-loki:3100
    isDefault: true
    editable: false
    jsonData:
      maxLines: 1000
      timeout: 60
