# Shared Sonar defaults for platform projects.
# Copy to your app repo root or merge keys into your own sonar-project.properties.
# Optional: fetch in CI from this configs repo via GitLab raw URL.

sonar.sourceEncoding=UTF-8
sonar.exclusions=**/node_modules/**,**/.git/**,**/dist/**,**/build/**,**/coverage/**,**/.vols/**
