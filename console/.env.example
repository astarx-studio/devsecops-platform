# Local `pnpm dev` — BFF proxies to Management API (compose host port 13000).
# Copy values from root .env; do not commit real secrets.
API_KEY=change-me-management-api-key
MANAGEMENT_API_GRAPHQL_URL=http://localhost:13000/graphql
